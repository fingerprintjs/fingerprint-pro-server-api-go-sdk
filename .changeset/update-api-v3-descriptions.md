---
"fingerprint-pro-server-api-go-sdk": patch
---

**events**: Require `LabelsInner.Label`, clarify `ProxyDetails.ProxyType` values, default the `Reverse` search filter to `false`, and update `VisitorsGetResponse` and `GetVisits` documentation to note that `visits` now contains at most one item
